<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.sun.newlanguage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.SendActivity"></activity>
        <activity
                android:name=".activity.CollapseActivity"
                android:theme="@style/NoActionBar"></activity>
        <activity android:name=".activity.WebviewActivity"></activity>
        <activity
                android:name=".activity.ViewPagerActivity"
                android:theme="@style/full"></activity>
        <activity android:name=".activity.ProgressActivity"></activity>
        <activity android:name=".activity.RotateActivity"></activity>
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.AddActivity"></activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>